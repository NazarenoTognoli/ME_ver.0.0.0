// vite.config.js
import { defineConfig } from 'vite';

export default defineConfig({  
  // Especifica el puerto en el que se ejecutará el servidor de desarrollo
  server: {
    port: 3000
  },

  // Otras opciones de configuración según sea necesario
});